# Launch Tweet

🚀 Just shipped quickstat — zero-config JSON statistics from your terminal

What it does:
→ Instant count, mean, median, std dev on any JSON array
→ Outlier detection built-in
→ Pipe-friendly: `cat data.json | npx quickstat`

Free and open source:
npx quickstat data.json

GitHub: https://github.com/stephenywilson/quickstat
